/*import React, { Component } from 'react';

class CityItem extends Component {
    render() {
      return this.state.cities.map((city) => 
        (<li key={city._id} className="citiesPage-list">{city.name + " " + city.country}</li>
        ));
    
  }
}

export default CityItem;*/